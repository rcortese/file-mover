
version: "3.8"

services:
  file-mover:
    image: rcortese/file-mover:latest
    # Change the paths below to match your local directories
    volumes:
      - <source_folder>:/source_folder
      - <destination_folder>:/destination_folder
